// 🔮 타로냥 - 메인 JS
// config.js may set window.__TARONYANG_CONFIG__.apiBase for cross-origin deployments (e.g. GitHub Pages)
const API_BASE =
  (window.__TARONYANG_CONFIG__ && window.__TARONYANG_CONFIG__.apiBase) ||
  (window.location.origin + '/api');

// TODO: Phase 2에서 구현
console.log('🔮 타로냥 로딩 완료');
